#include "megasena.h"
#include <stdio.h>

int main(){
    int quantidade_de_dezenas_por_aposta; //usar depois pra alguma coisa que ainda não sei
    menu();
    solicitacoes(&quantidade_de_dezenas_por_aposta);
    return 0;
}